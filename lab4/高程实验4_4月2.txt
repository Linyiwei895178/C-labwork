实验4、 数据保护与共享

实验目的：
1. 掌握数据共享与保护机制：作用域、生命周期、类、命名空间等,static、友元
3. 掌握const保护: 常对象、常成员、常引用

实验内容：
参照《C++语言程序设计》学生用书，完成以下实验：

--第4章 实验4  类与对象--
实验任务：
1. 作用域、生命周期：类的静态static成员
--第5章 实验5 数据的共享与保护 --
   练习学生用书第5章习题5-7的程序，分析运行结果。问：
 （1）代码中istAge(age)的含义；
 （2）讨论静态变量 numOfCats与全局变量的区别。
 （3）int getNumofCats() 前的static可以去掉吗？
 （4）telepathicFunction() 除了原本作为孤立函数，还可以怎样设计？

2.友元：
   分析学生用书第5章习题5-4，讨论class fuel中的friend class engine 有必要加吗？


3. 数据的共享与保护：const保护
  （1）常对象：
   练习课本例5-7.cpp, 讨论去掉void R::print() const{ 语句行中的const 可以吗？为什么？
   在R类中增加一个成员函数change()如下，尝试用a对象和b对象分别调用它可以吗？为什么？
    change(int x1,int x2){ 
	r1=x1;
	r2=x2;
	}

 （2）常成员：
  练习课本例5-8.cpp, 讨论将A::A(int i) : a(i) { } 语句改为 A::A(int i) :{ a=i; }可以吗？为什么？
       其中的 static const int b; 能在类内初始化吗？  

 （3）常引用：
  练习课本例5-9.cpp, 尝试将友元函数 dist(const Point &p1, const Point &p2) 的两处声明和类外定义的形参都取消const可以吗？为什么？
  尝试将main()函数中 const Point myp1(1, 1), myp2(4, 5); 语句中的const 取消，程序能正常运行吗？为什么？

4. 解释附件 引用2.cpp中的问题。

探索：
探讨在面向对象的程序设计中，有哪些可以替代全局变量的共享数据方案？
针对实验中出现的问题，写出你的思考，编写程序探索答案。
